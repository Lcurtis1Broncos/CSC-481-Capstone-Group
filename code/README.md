# Source Code

Place all project source code in this folder. Commit working code at least weekly.

## Current tool

`ntfs_image_reader.py` reads the first 512 bytes of a disk image without
modifying it. Run it from the repository root with:

```powershell
python code/ntfs_image_reader.py path/to/image.dd
```
